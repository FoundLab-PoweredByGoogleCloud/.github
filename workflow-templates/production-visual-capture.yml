name: Production Visual Capture

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  visual-capture:
    name: Capture production UI evidence
    uses: FoundLab-PoweredByGoogleCloud/.github/.github/workflows/visual-capture.yml@main
    with:
      url: https://REPLACE_WITH_PRODUCTION_URL
      routes: /,/login,/pricing
      artifact-name: production-visual-capture
      wait-ms: "2500"
      full-page: "true"
